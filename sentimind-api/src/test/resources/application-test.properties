# Configuración específica para tests con Testcontainers
# IMPORTANTE: Testcontainers sobreescribirá el datasource con @DynamicPropertySource
# NO configurar datasource aquí

# Driver PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JPA/Hibernate - Será configurado por Testcontainers
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Deshabilitar otras configs
spring.flyway.enabled=false
spring.h2.console.enabled=false
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false

# Logging
logging.level.com.sentimind.sentimind_api=INFO
logging.level.org.testcontainers=INFO
logging.level.com.github.dockerjava=WARN
logging.level.org.hibernate=WARN