# ===============================
# Database Configuration - H2 (TEMPORAL para desarrollo local)
# ===============================
spring.datasource.url=jdbc:h2:mem:devdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ===============================
# JPA / Hibernate - H2
# ===============================
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================
# SQL Initialization
# ===============================
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# ===============================
# Database Configuration - PostgreSQL (COMENTADO - Para producción)
# ===============================
# spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.hibernate.ddl-auto=validate
spring.profiles.active=prod

# ===============================
# Configuración de Integración
# ===============================
ai.integration.enabled=false
ai.api.url=http://localhost:5000/predict

# ===============================
# Security
# ===============================
security.user.name=${SECURITY_USER:usuario}
security.user.password=${SECURITY_PASSWORD:123456}